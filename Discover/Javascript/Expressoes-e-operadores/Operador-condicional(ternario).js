// Operador Condicional (Ternário)

// Dependendo da condição, nós receberemos valores diferentes

// Condição então valor 1 se não valor 2
// condition ? value1 : value2

// Exemplos

// Café da manhã top
/*
let pao = true
let cafe = false

const belocafedamanha = pao || cafe ? "Pog" : "meme"
console.log(belocafedamanha)
*/
// Maior que 18

let age = 254

const comparacao = age >= 18  ? "Maior" : "Menor"
console.log(comparacao)